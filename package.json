{
  "name": "dwp-abbrev",
  "private": true,
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build": "bunx tsc --noemit && bun build index.html --outdir ./dist --target browser"
  },
  "devDependencies": {
    "@types/bun": "1.3.11"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
